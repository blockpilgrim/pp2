###############################################################################
# Auth.js / NextAuth core
###############################################################################
# 32-char cryptographically-random secret – generate with:
#   openssl rand -base64 32
AUTH_SECRET=""

# Base-URL where your app is reachable
NEXTAUTH_URL="http://localhost:3000"

###############################################################################
# Microsoft Entra ID  (formerly Azure AD)  —  **v5 auto-inference ready**
###############################################################################
# These 3 variables are all NextAuth needs; the library will map them to the
# provider’s clientId, clientSecret, and issuer automatically.
AUTH_MICROSOFT_ENTRA_ID_ID=""
AUTH_MICROSOFT_ENTRA_ID_SECRET=""
# “organizations”, “common”, “consumers”, or a tenant GUID all work:
AUTH_MICROSOFT_ENTRA_ID_ISSUER=""

###############################################################################
# Dataverse / BFF (back-end-for-front-end)  – placeholders
###############################################################################
DATAVERSE_URL=""
DATAVERSE_CLIENT_ID=""
DATAVERSE_CLIENT_SECRET=""
DATAVERSE_TENANT_ID=""
